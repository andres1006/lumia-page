---
import { getLangFromUrl, useTranslations } from "../i18n";
import SocialIcons from "./react/SocialIcons";
import UltraOptimizedImageRef from "./UltraOptimizedImageRef.astro";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<footer class="bg-black text-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
      <!-- Logo and Description -->
      <div>
        <div class="flex items-center space-x-3 mb-6">
          <UltraOptimizedImageRef
            src="/optimized-images/img-lumia-ultra/logo-lumia-optimized.png"
            alt="LUMIA"
            class="w-12 h-12"
            quality="high"
            loading="lazy"
          />
          <div>
            <span class="text-2xl font-light text-white">LUMIA</span>
            <div class="text-sm text-gray-400 -mt-1">
              {
                lang === "es"
                  ? "Odontología Especializada"
                  : "Specialized Dentistry"
              }
            </div>
          </div>
        </div>
        <p class="text-gray-300 mb-6 max-w-sm">
          {
            lang === "es"
              ? "Transformamos sonrisas con tecnología de vanguardia y atención personalizada de clase mundial."
              : "We transform smiles with cutting-edge technology and world-class personalized care."
          }
        </p>
        <SocialIcons client:load />
      </div>

      <!-- Quick Links -->
      <div>
        <h3 class="text-lg font-medium text-white mb-6">
          {lang === "es" ? "Enlaces" : "Links"}
        </h3>
        <ul class="space-y-3">
          <li>
            <a
              href={lang === "es" ? "/" : "/en"}
              class="text-gray-300 hover:text-white transition-colors"
            >
              {t("nav.home")}
            </a>
          </li>
          <li>
            <a
              href={lang === "es" ? "/nosotros" : "/en/about"}
              class="text-gray-300 hover:text-white transition-colors"
            >
              {t("nav.about")}
            </a>
          </li>
          <li>
            <a
              href={lang === "es" ? "/servicios" : "/en/services"}
              class="text-gray-300 hover:text-white transition-colors"
            >
              {t("nav.services")}
            </a>
          </li>
          <li>
            <a
              href={lang === "es" ? "/galeria" : "/en/gallery"}
              class="text-gray-300 hover:text-white transition-colors"
            >
              {t("nav.gallery")}
            </a>
          </li>
          <li>
            <a
              href={lang === "es" ? "/contacto" : "/en/contact"}
              class="text-gray-300 hover:text-white transition-colors"
            >
              {t("nav.contact")}
            </a>
          </li>
        </ul>
      </div>

      <!-- Contact Info -->
      <div>
        <h3 class="text-lg font-medium text-white mb-6">
          {lang === "es" ? "Contacto" : "Contact"}
        </h3>
        <div class="space-y-4">
          <div>
            <p class="text-gray-400 text-sm mb-1">
              {
                lang === "es"
                  ? "CC Fundadores, Local 9908, 2do Piso"
                  : "CC Fundadores, Local 9908, 2nd Floor"
              }
            </p>
            <a
              href="tel:+573164052829"
              class="text-white hover:text-gray-300 transition-colors text-sm block"
            >
              +57 (316) 405-2829
            </a>
          </div>

          <div>
            <p class="text-gray-400 text-sm mb-1">
              {t("contact.schedule")}
            </p>
            <p class="text-white text-sm whitespace-pre-line">
              {
                lang === "es"
                  ? "Lun - Vie: 9:00 AM - 7:00 PM\nSáb: 9:00 AM - 12:00 PM"
                  : "Mon - Fri: 9:00 AM - 7:00 PM\nSat: 9:00 AM - 12:00 PM"
              }
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Bottom Border -->
    <div class="border-t border-gray-800 mt-12 pt-8">
      <div class="flex flex-col md:flex-row justify-between items-center">
        <p class="text-gray-400 text-sm">
          © 2024 LUMIA Odontología Especializada. {
            lang === "es"
              ? "Todos los derechos reservados."
              : "All rights reserved."
          }
        </p>
        <div class="flex space-x-6 mt-4 md:mt-0">
          <a
            href="#"
            class="text-gray-400 hover:text-white transition-colors text-sm"
          >
            {lang === "es" ? "Privacidad" : "Privacy"}
          </a>
          <a
            href="#"
            class="text-gray-400 hover:text-white transition-colors text-sm"
          >
            {lang === "es" ? "Términos" : "Terms"}
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>
